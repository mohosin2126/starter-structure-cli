# create-starter-structure-cli

Use `starter-structure-cli` through the standard npm create flow:

```bash
npm create starter-structure-cli@latest my-app
```

This package is a thin wrapper around `starter-structure-cli`.

You can also run the main CLI directly:

```bash
npx starter-structure-cli my-app
```
